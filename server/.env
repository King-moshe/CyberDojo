MONGO_URI=mongodb://localhost:27017/cyberdojo
PORT=3000
NODE_ENV=development

# Comma-separated list of allowed hosts for attack/http executor
# Example: ATTACK_ALLOWLIST=localhost,127.0.0.1,example.com
ATTACK_ALLOWLIST=localhost,127.0.0.1

# Behavior when an attack step is blocked by the allowlist: 'continue' or 'fail'
ATTACK_BLOCK_BEHAVIOR=fail