## Client environment variables for Vite
# Vite loads variables prefixed with VITE_ into import.meta.env

# Base URL for API requests (defaults to "/api" which proxies to the server in dev)
VITE_API_BASE_URL=/api

# App environment (development | production)
VITE_APP_ENV=development

# Enable SSE endpoints (used by alerts and runs streaming). Set to "false" to disable.
VITE_ENABLE_SSE=true
